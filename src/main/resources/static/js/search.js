const searchForm = document.getElementById('search-form');

// new plan: the form will make a get request to the server & the server will find the closest matching station\
// but keep this for now in case I also want to trigger something else with the form
